# 仓库 Agent 执行规则

本文件是 `my-pi-extension` 的第四层 Agent 执行入口。项目只使用一个 GitHub monorepo：第一至第四层可以进入 GitHub 正式历史；第五层只保留本地连续性材料，不能覆盖正式层。

## 必读顺序

开始任务前按需读取：

1. `docs/README.md`：确定五层路由和真理源。
2. `docs/01-产品定义/产品上下文摘要.md`：快速加载 L1 核心语义；有歧义时回到权威定义。
3. `docs/01-产品定义/领域术语.md`：读取 L1 权威产品定义。
4. 与任务相关的 L1 Extension 产品规范或架构决策。
5. `docs/02-产品实现/README.md`、相关 L2 机制设计及 `packages/*/src`、`packages/*/test`。
6. 涉及 package、配置或目录时读取第三层。
7. 涉及协作、验证、交付或文档变更时读取第四层。

`docs/归档/` 只能用于了解历史背景。不得从归档中恢复已被活跃文档或代码替代的规则。

## 五层路由

| 层级 | 责任 | 正式入口 | 写回条件 |
| --- | --- | --- | --- |
| 第一层 | 产品必须保证什么；Core 共享语义与 Extension 特有流程 | `docs/01-产品定义/` | 产品目标、用户行为、Stage/Guard/Acceptance 语义或产品级架构变化 |
| 第二层 | Runtime 当前怎样实现、真实做到了什么 | `docs/02-产品实现/`、`packages/*/src/`、`packages/*/test/` | 实现机制、源码、测试或当前技术事实变化 |
| 第三层 | monorepo 组织、package、构建和项目默认配置 | `docs/03-项目落地/`、package/config 文件 | 目录、package、安装、构建、模型策略默认值变化 |
| 第四层 | GitHub 协作、验证、交付和 Agent 规则 | `docs/04-仓库治理/`、`AGENTS.md` | 共享工作方式或质量门禁变化 |
| 第五层 | 当前本地任务、临时 trace、handoff 和 session 连续性 | 本地 ignored 路径 | 只服务当前工作现场；默认不提交 |

每个文件只能有一个主要责任。一个文件同时承担两个主要层级时，先拆分再写回，不要按标题、目录名或文件类型强行归类。

## 真理源与冲突规则

- 第一层是预期产品语义的权威：Core 拥有共享规则，各 Extension 拥有入口特有流程。
- L1 产品上下文摘要是权威定义的派生产物，仍属于 L1；不得独立引入规则。
- 第二层源码和测试是当前运行事实的权威；L2 机制文档不得重新定义产品语义。
- 第一层与第二层冲突时，记录文档漂移；不得静默修改其中一方来掩盖冲突。
- 第三层只承载已采纳的仓库默认。实验性配置留在第五层，直到明确采纳。
- 第四层约束共享执行和交付；本地备注不得绕过这些门禁。
- 第五层和归档都不是正式真理源。
- 文档状态为 `TODO`、`ALIGNING` 或 `REVIEWING` 时，不得当作已采纳契约。

## 文档路由门禁

修改前先确定 owner：

- 已有产品行为的 Bug、回归或优化：若产品契约不变，只更新 L2 实现和测试；若预期行为也需改变，更新拥有该行为的 L1 Core/Extension，并同步 L2 或记录 drift。不要创建平行 fix 真理源。
- 新 Extension 或新用户旅程，先在对应 L1 Extension 建立产品规范，再设计和实现 L2。
- 纯实现重构且产品行为不变，只更新 L2，并在结果中说明 L1 无需变化。
- L1 变化不等于 L2 已实现；尚未实现时必须在 L2 drift 表中明确记录。
- package、目录、安装、构建或项目策略变化，更新第三层。
- Agent、review、验证或 GitHub 流程变化，更新第四层。
- 调查、比较或评审原文进入 `docs/归档/`；结论只有明确采纳并写入活跃层后才生效。
- 新的跨层产品/技术方案在采纳前必须经过至少两条独立评审轴：产品语义/指标轴和实现/运营轴；评审 Agent 只报告 finding，不修改方案。主 Agent 必须保留“当前事实、目标设计、已知 drift”和分歧，不能用自己的总结替代独立评审。

如果多个文档都可能拥有同一行为，停止创建新文档，先确认唯一 owner 或拆分混合责任。

## 实现规则

- 使用现有 TypeScript workspace 和 package 边界，不预先创建大型 shared core。
- 方案评审通过不等于实现完成；方案中声明的每个事件、状态、指标和验收条件都必须在对应 L2 源码/测试中有真实产生点或明确 drift。
- Worker 只能提交当前 Node 的 Artifact；只有 Runtime 可以改变 Workflow Run 状态。
- skill routing 和 tool allowlist 是不同控制面。Skill 不自动扩大工具权限。
- Pi 工具限制不是 OS sandbox；不得在文档或代码中夸大隔离保证。
- 修改行为时补充或更新最接近该行为的测试。
- 不提交 secrets、凭证、真实用户数据、生成物、临时 trace 或 session 状态。
- 不回退或覆盖与当前任务无关的用户改动。

## GitHub 与验证

- GitHub Issue/PR 是共享工作记录；仓库活跃文档和代码仍是对应层的正式真理源。
- 涉及新产品能力、跨层技术方案、数据采集、外部服务或发布的 PR，必须附独立产品语义评审和独立实现/运营评审结论；未解决的 P0/P1 finding 或未确认的关键产品决策不得进入实现。
- PR 必须说明影响层级、修改的正式入口、验证证据和剩余风险。
- **所有 coding 修改必须经过 `implementer` → `code_reviewer` 流程**：只要任务需要产生源码、测试、脚本、构建/运行配置或 UI 的代码 diff，不论入口名称、规模、技术栈或是否为简单修复，都必须先由 `implementer` subagent 执行代码改动，再启动独立的 `code_reviewer` agent 进行代码评审；不因改动规模小、测试已通过或实现者自检通过而跳过。纯文档、纯调查、只读评审或未修改代码的任务不适用本条。
- `implementer` 负责所有 coding 改动及自检，不能替代 `code_reviewer`；`code_reviewer` 负责独立检查需求符合度、仓库规范、状态/合同/版本/恢复/审计风险和测试覆盖。
- `code_reviewer` 发现 P0/P1 或需要修复的 P2 后，必须由实现者修复，并对修复后的代码再次执行 `code_reviewer`；未完成复审不得将代码实现标记为完成。
- 评审收敛边界：P2 坏味道类 finding 允许接受不修并记入 backlog/drift，不强制触发新一轮复审；同一变更最多复审 3 轮，超出后剩余 finding 记入 drift 表并交人工决策，不再自动循环。
- 合并前至少运行：

```bash
npm test
npm run typecheck
```

- 文档移动或链接变化时，检查全部仓库内 Markdown 相对链接。
- 无法运行的检查必须在交付说明中写明原因；不得把未执行描述为通过。

## 完成检查

结束任务前确认：

1. 变更已写回正确且唯一的层级 owner；Feature/Fix 特有流程没有塞回 L1 Core。
2. L1 产品意图与 L2 当前实现之间没有未报告 drift；L1 摘要没有超出权威定义。
3. 第五层或归档内容没有被误升为正式规则。
4. 测试、类型检查和相关文档链接检查已通过，或阻塞已明确报告。
5. 没有生成物、临时状态或敏感信息进入 Git 历史。
